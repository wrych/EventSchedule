// import { fileURLToPath } from 'url'
import path from 'path'

// const sourcesPath = dirname(fileURLToPath(import.meta.url)) // use for es6 modules
export const rootPath = path.join(__dirname, '../')
